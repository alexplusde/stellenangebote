# Klasse `Apply`

Die Klasse `Apply` repräsentiert die Bewerbung eines Bewerbers auf ein Stellenangebot in der Datenbank. Sie erbt von der Klasse `rex_yform_manager_dataset` und stellt Methoden zur Verfügung, um auf die Daten der Bewerbung zuzugreifen und diese zu manipulieren.
